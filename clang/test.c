#include <stdio.h>

int main(){

    int oldday = 2;

    if(oldday == 0){
        printf("oldday : %d\n", oldday);
        oldday++;
    }else if(oldday == 1){
        printf("oldday : %d\n", oldday);
        oldday++;
    }else if(oldday == 2){
        printf("oldday : %d\n", oldday);
        oldday++;
    }else{
        printf("oldday : %d\n", oldday);
        oldday++;
    }
    printf("oldday : %d\n", oldday);

}